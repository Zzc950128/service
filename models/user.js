'use strict'

const userConfig = {
    name: 'user',
    schema: {
        uid: Number,
        userName: String,
        password: String,
        name: String,
        role: String,
    }
}

module.exports = userConfig